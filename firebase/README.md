# Firebase Configurations & Emulator Setup

This directory houses Firebase configuration files, emulator data exports, Firestore indexes, security rule sets, and deployment configs.

## Features
- Firestore Security Rules & Indexes
- Firebase Emulator Suite configuration
- Storage Security Rules
